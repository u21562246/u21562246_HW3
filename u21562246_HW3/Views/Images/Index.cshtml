@using u21562246_HW3.Models
@model  List<FileModel>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


@foreach(var ImageInModel in Model)
 {
    <div class="row">
        <div class="col-lg-4">
            <img src="~/Media/Images/@ImageInModel.FileName" style="height:350px" />
            <p>@ImageInModel.FileName</p>
            <button type="button" class="btn btn-success">@Html.ActionLink("Download", "Download", new { FileName = ImageInModel.FileName })</button>
            <button type="button" class="btn btn-danger">@Html.ActionLink("Delete", "Delete", new { FileName = ImageInModel.FileName })</button>
        </div>
    </div>
 }